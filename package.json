{
  "name": "shieldnet",
  "version": "1.0.0",
  "description": "On-chain smart contract audit bounty board for BOT Chain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network botchain_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network botchain_mainnet"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "hardhat": "^2.19.0"
  }
}
